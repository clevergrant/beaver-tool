# Web interface port (default: 3000)
TB_PORT=3000

# Timberborn HTTP API URL (default: http://localhost:8080/api)
# Change the port if your game uses a different one
TB_GAME_API=http://localhost:8080/api

# Discord bot token (optional — leave empty to disable)
DISCORD_BOT_TOKEN=
